console.log(b);
console.log(a);
let a = 10;
var b = 52;

// ? Temporal Dead Zone | Namaste JS EP.8
// This is what the Temporal Dead Zone is: the term to describe the state where variables are un-reachable. They are in scope, but they aren't declared.

// The let and const variables exist in the TDZ from the start of their enclosing scope until they are declared.


